server:
  port: 8761
eureka:
  instance:
      hostname: discovery-server
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      default-zone: http://${discoveryserver.instance.hostname}:${server.port}/eureka/
